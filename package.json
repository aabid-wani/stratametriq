{
  "name": "stratametriq",
  "version": "1.4.3",
  "private": true,
  "description": "StrataMetriq - Architecture Intelligence & Pre-Deployment Safety in VS Code",
  "workspaces": [
    "extension",
    "scanner",
    "runtime",
    "ai",
    "dashboard",
    "shared",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
